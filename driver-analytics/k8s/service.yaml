apiVersion: v1
kind: Service
metadata:
  name: driver-analytics
spec:
  selector:
    app: driver-analytics
    harness.io/color: blue
  ports:
    - port: 7002
      targetPort: 7002
      protocol: TCP
